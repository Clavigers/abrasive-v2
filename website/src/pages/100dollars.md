bazel gripes 
100 dollars goes to the first person who... 
can cross compile for windows from linux using rules_rust another hundred if the built image uses a windows dll
can bazel build a cargo crate publishable on crates.io
can run cargo benchmark in a reasonable way via bazel
can get the wonderful incredible extremely fast incremental caching that rust is designed for (read -C incremental not crate-wise incremental)
CAN EVEN MAKE IT TRULY HERMETIC
doing pipelining without killing rustc mid process
Build a workspace whose build.rs does anything mildly interesting
Use any -Z flag without forking the toolchain definition
I wrote all of these without even glancing at a github issue.