<aside class="paper-note">
  <slot />
</aside>

<style>
  .paper-note {
    grid-area: paper-note;
    max-width: 380px;
    margin-top: 4rem;
    background-color: #e0cfa7;
    background-image:
      url("data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='240' height='240'><filter id='n'><feTurbulence type='fractalNoise' baseFrequency='0.85' numOctaves='2' seed='4'/><feColorMatrix values='0 0 0 0 0.2  0 0 0 0 0.15  0 0 0 0 0.08  0 0 0 0.18 0'/></filter><rect width='100%' height='100%' filter='url(%23n)'/></svg>"),
      radial-gradient(ellipse at top left, rgba(255, 240, 210, 0.35), transparent 60%),
      radial-gradient(ellipse at bottom right, rgba(140, 110, 70, 0.22), transparent 55%);
    color: #2b2a27;
    padding: 1.5rem 1.25rem 1rem;
    font-family: "Caveat", Georgia, "Times New Roman", serif;
    font-size: 1.625rem;
    font-weight: 500;
    line-height: 1.35;
    transform: rotate(-1.2deg);
    box-shadow:
      3px 5px 14px rgba(0, 0, 0, 0.38),
      inset 0 0 40px rgba(120, 90, 50, 0.15);
    border-radius: 2px;
    position: relative;
    z-index: 1;
  }
  .paper-note::before,
  .paper-note::after {
    content: '';
    position: absolute;
    width: 77px;
    height: 24px;
    background: linear-gradient(
      180deg,
      rgba(255, 255, 255, 0.35) 0%,
      rgba(240, 240, 240, 0.45) 50%,
      rgba(220, 220, 220, 0.3) 100%
    );
    mix-blend-mode: screen;
    filter: drop-shadow(0 1px 2px rgba(0, 0, 0, 0.18));
    clip-path: polygon(
      0% 0%, 100% 0%,
      95% 16%, 100% 32%, 94% 48%, 100% 64%, 95% 80%, 100% 100%,
      0% 100%,
      5% 84%, 0% 68%, 6% 52%, 0% 36%, 5% 20%
    );
    z-index: 2;
  }
  .paper-note::before {
    top: -13px;
    left: -25px;
    transform: rotate(-38deg);
  }
  .paper-note::after {
    bottom: -13px;
    right: -25px;
    transform: rotate(-38deg);
  }
  .paper-note :global(p) {
    margin: 0;
  }
  .paper-note :global(a) {
    color: #0858c0;
    text-decoration: underline;
    text-underline-offset: 2px;
  }
  .paper-note :global(a:hover) {
    color: #05458f;
  }
  .paper-note :global(mark) {
    background: linear-gradient(
      100deg,
      rgba(255, 155, 50, 0) 0%,
      rgba(255, 155, 50, 0.7) 5%,
      rgba(255, 160, 60, 0.8) 45%,
      rgba(255, 170, 50, 0.78) 75%,
      rgba(255, 155, 50, 0.7) 94%,
      rgba(255, 155, 50, 0) 100%
    );
    color: #4a2818;
    padding: 0.05em 0.2em;
    mix-blend-mode: darken;
    display: inline-block;
    transform: rotate(-1.5deg);
    transform-origin: center;
  }
</style>
